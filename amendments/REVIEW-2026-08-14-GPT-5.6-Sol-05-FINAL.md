# External Adversarial Review 05 — Final Confirmation (v4)

**Reviewer:** GPT-5.6 Sol (High), OpenAI lineage — G7 external review.
**Date:** 2026-08-14. **Target:** AMD-2026-08-14-KHI-EXECUTION-v4-DRAFT.md at
commit 08b0572.
**Provenance:** conducted in ChatGPT by the operator; transcribed verbatim
below; committed unedited.
**Verdict:** P3-V5 CONFIRMED; P3-V13 CONFIRMED; A28.5 consistency edit
CONFIRMED SOUND; substantive invariant ("no other substantive protocol text
differs from v3") PASSES. **One BLOCKING finding:** v4's front-matter
assertion "No other text differs from v3" is literally false (title/status,
freeze-prerequisite wording, and closing reviewer-instructions also changed).
**Disposition:** ACCEPTED; `AMD-2026-08-14-KHI-EXECUTION-v5-DRAFT.md` corrects
only the self-description to the substantive-invariant phrasing the reviewer
stated passes, and replaces the closing instructions with a certification
summary. Zero protocol text (A20–A31, R-1, R-2) changes from v4.

---

GPT-5.6 Sol — August 14, 2026.

1. P3-V5 — CONFIRMED. The four required constructions are implemented correctly: A20 lottery, Stage-1 stream, Stage-2 stream, and A22 decoy lottery all use the exact `SHA-256(JCS([...]))` preimages I required. A20.9 also contains the required definition that `JCS(x)` is RFC-8785 canonical JSON serialized as UTF-8, with no additional prefix/suffix/delimiter/normalization, and defines `null_id` exactly as `"N1"` or `"N2"`.
2. P3-V13 — CONFIRMED. `UNSCOREABLE_N2_SUPPORT` is present in the closed terminal-status vocabulary, and the frozen first-match precedence is exactly the required chain: Stage-1 raw failure → N1-support → N2-support → decoy-stratum → QC → both Stage-2 raw nulls fail → N1 only → N2 only → Holm → decoy-rank → pass. The restriction that only `S2_FAIL_*` statuses are SPNA-eligible is preserved.
3. A28.5 consistency edit — CONFIRMED SOUND. `SHA-256(JCS(triple_id))` is consistent with A20.9 because `triple_id` is defined there as the JSON array `[khipu_id, grouping_id, doc_id]`; applying JCS to that array gives a single unambiguous RFC-8785 UTF-8 preimage before hashing. This is preferable to the v3 `SHA-256(triple_id)` formulation and introduces no domain-separation ambiguity because the hash is used only for deterministic table ordering, not as one of the lottery/RNG domains.
4. Literal v3→v4 identity outside those edits — NOT CONFIRMED. I performed a line-level diff against the v3 artifact I reviewed. No additional substantive A20–A31 protocol rule changed, but the statement "no other line differs from v3" is literally false. In addition to P3-V5, P3-V13, and A28.5, v4 changes:
   * the title/status and introductory version-history text;
   * the freeze-prerequisite wording from "external confirmation pass 3 verifying the 14 edits" to confirmation of the two P3 fixes plus A28.5;
   * the closing reviewer-instructions section, replacing the pass-3 instructions with the present final-confirmation instructions.
The v3 front matter explicitly had different title/version and freeze-confirmation language. The v4 front matter contains the replacement metadata and itself says "No other text differs from v3." The v3 closing instructions likewise differ from the new v4 closing instructions.

These extra differences are administrative/version-control text, not changes to the scientific or inferential protocol. But because item (4) asked me to certify the literal claim that no other line differs, I cannot certify it as written. If the intended invariant is "no other substantive protocol text differs from v3," that invariant passes.
BLOCKING FINDING: the literal no-other-line-diff assertion is false because of the identified front-matter and closing-instruction edits.
