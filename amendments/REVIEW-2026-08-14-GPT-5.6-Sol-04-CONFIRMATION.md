# External Adversarial Review 04 — Confirmation Pass 3 (v3 consolidated + GOVERNANCE.md)

**Reviewer:** GPT-5.6 Sol (High), OpenAI lineage — G7 external review.
**Date:** 2026-08-14. **Target:** AMD-2026-08-14-KHI-EXECUTION-v3-DRAFT.md and
GOVERNANCE.md at commit 4fbfb17, against verification review 03.
**Provenance:** conducted in ChatGPT by the operator; transcribed verbatim
below; committed unedited.
**Verdict:** 12 of 14 items IMPLEMENTED; **2 BLOCKING** consolidation seams —
P3-V5 (hash preimages not converted to the RFC-8785 JCS constructions;
null_id undefined) and P3-V13 (missing `UNSCOREABLE_N2_SUPPORT` terminal
status) — each with exact replacement text.
**Disposition:** both ACCEPTED; implemented verbatim in
`AMD-2026-08-14-KHI-EXECUTION-v4-DRAFT.md`, which also applies the same JCS
rule to the A28.5 table-ordering hash (SHA-256(JCS(triple_id))) as a flagged
consistency edit.

---

GPT-5.6 Sol — August 14, 2026.

1. V1 — IMPLEMENTED. A21 now uses the required probability-weighted N2 support condition D⋅mindMd>1000m, explicitly rejects ∑dMd as the effective-support denominator, and preserves p=1 treatment for unscoreables. This is the substantive replacement required by V1.
2. V3 — IMPLEMENTED. R-1 now explicitly says the frozen requirement was ≥100 decoys, the amendment reduces the document-identity panel to 25, and that this is a substantive weakening, not a clarification or narrowing. It also makes operator signature the ratification act. The replacement language is substantively verbatim; the added parenthetical context after it does not weaken the disclosure.
3. V4 — IMPLEMENTED. `E_raw` is now the full enumerated cross-product, while `E` is explicitly the registered scoring extent after the deterministic one-million-triple fallback; m=∣E∣, and excluded raw triples receive no test status or p-value. A28 correctly limits its terminal-status requirement to triples in `E`.
4. V5 — NOT FULLY IMPLEMENTED; BLOCKING CONSOLIDATION INCONSISTENCY. The requested canonicalization text was inserted at A20.9, but the consolidated document still specifies the actual hash preimages using raw concatenation:
   * `SHA-256(triple_id ∥ "KHI-A20-LOTTERY-20260814")`;
   * `SHA-256("KHI-S1" ∥ triple_id ∥ null_id)`;
   * `SHA-256("KHI-S2" ∥ triple_id ∥ null_id)`;
   * `SHA-256(doc_id ∥ "KHI-A22-LOTTERY-20260814")`.
A20.9 simultaneously says every lottery and RNG seed uses RFC-8785 canonical JSON serialized as UTF-8. Those are not the same specification: concatenating a canonical JSON object/string with another string does not itself produce RFC-8785 canonical JSON. `null_id` is also never assigned its exact canonical values. Thus two conforming implementations can hash different byte sequences.
Required fix: replace the four hash/seed expressions with the following exact constructions, and add the `null_id` definition:
For all domain-separated hashes, `JCS(x)` means RFC-8785 canonical JSON serialized as UTF-8, and the SHA-256 input is exactly `JCS(x)` with no prefix, suffix, delimiter, or additional normalization. `null_id` is exactly `"N1"` or `"N2"`.
A20 scoring lottery: `SHA-256(JCS(["KHI-A20-LOTTERY-20260814", khipu_id, grouping_id, doc_id]))`.
Stage-1 stream: `SHA-256(JCS(["KHI-S1", khipu_id, grouping_id, doc_id, null_id]))`.
Stage-2 stream: `SHA-256(JCS(["KHI-S2", khipu_id, grouping_id, doc_id, null_id]))`.
A22 decoy lottery: `SHA-256(JCS(["KHI-A22-LOTTERY-20260814", doc_id]))`.
A20.9 may otherwise remain unchanged.
5. V6 — IMPLEMENTED. The bins are exactly `{1532–1581, 1582–1631, 1632–1681, 1682–1700}`, with comparison confined to the same bin; no bin permits a >50-year separation.
6. V7 — IMPLEMENTED. The metadata codebook now freezes and is externally anchored before inspection of the first item-level enumeration result; unseen, missing, conflicting, and multivalued load-bearing values become `UNRESOLVED`, and post-enumeration remapping is prohibited. This implements the V7 replacement text without substantive drift.
7. V8 — IMPLEMENTED. A25.6 mechanically defines coverage, counts all five field states once, freezes tie-breaking by publication date and then normalized citation identifier, prohibits use of numeric values/khipu comparison, and requires deposit before scoring.
8. V9 — IMPLEMENTED. The invocation wrapper now requires the pre-invocation commitment to be pushed and remotely verified before the call, and the raw result/error to be pushed before another invocation can occur. Silent retry and local-history cherry-picking routes identified in V9 are closed.
9. V10 — IMPLEMENTED. A28 abandons the deferred fitted joint-null calibration for this run and uses only the preregistered independence reference and Fréchet range; A28.8 now points to the clause-4 reference bounds, not a nonexistent fitted E0[L]. The notation was typographically normalized from `E_0[L]` to `E₀[L]`; that is not substantive drift.
10. V11 — IMPLEMENTED. The amended GOVERNANCE now agrees with A27 on all three items requested:

* the canonical record is the anchored chain of exact signed `freeze/*` tags, not the default branch;
* R4 makes anchoring a prerequisite to freeze effectiveness and gives the former 72-hour language no provisional execution authority;
* the freeze procedure puts the frozen-manifest update into the same merged tree before the signed tag is created, and no gated action may occur until anchoring is complete.

A27 says the same thing. V11 is resolved.

11. V12 — IMPLEMENTED. A20.8 now makes `S_max` planning-only, prohibits selecting a survivor subset when realized survivors exceed capacity, suspends all Stage 2 until all survivors can be processed identically, and leaves E, m, and eligibility unchanged.
12. V13 — PARTIALLY IMPLEMENTED; BLOCKING TRANSCRIPTION SLIP. The requested precedence rule and `S2_FAIL_BOTH_NULLS` were added. However, V1 simultaneously made N2-support failure a distinct scoreability condition, while the consolidated terminal-status vocabulary contains `UNSCOREABLE_N1_SUPPORT` but no `UNSCOREABLE_N2_SUPPORT`. A21 says failure of either N1 or N2 support makes a triple unscoreable, and A30 separately requires reporting N1-support and N2-support unscoreables.

A screen-positive triple that passes N1 support but fails DminMd>1000m therefore has no legal terminal status despite the rule that every member of `E` receives exactly one.
Required fix: in A28.3 replace the status/precedence sentence with:
Every triple in E receives exactly one terminal status from: `S1_ONLY_FAIL_RAW`, `S2_FAIL_BOTH_NULLS`, `S2_FAIL_N1`, `S2_FAIL_N2`, `S2_FAIL_HOLM`, `S2_FAIL_DECOY_RANK`, `UNSCOREABLE_N1_SUPPORT`, `UNSCOREABLE_N2_SUPPORT`, `UNSCOREABLE_DECOY_STRATUM`, `UNSCOREABLE_QC`, `PASS_ADJUDICATION`. Precedence (frozen; first matching state wins): Stage-1 raw failure → N1-support → N2-support → decoy-stratum → QC → both Stage-2 raw nulls fail → N1 only → N2 only → Holm → decoy-rank → pass. Only `S2_FAIL_*` statuses are SPNA-eligible.

13. V14 — IMPLEMENTED. A28.2 now says the 9,999/9,999 rule is "matching A20.5 exactly." The obsolete standalone-A28 interaction note saying it "narrows" A20.5 was eliminated during consolidation rather than retained with stale wording. A20.5 and A28.2 now agree.
14. CONSOLIDATION CHECK — two defects found, both above. I found no additional cross-document numbering, Eraw/E, A28 future-use, R-1/R-2, Stage-1/Stage-2, A30 reporting, or GOVERNANCE/A27 contradiction introduced by the merge. The two defects are specifically consolidation/implementation seams: V5's canonical-JSON rule was merged without converting the four legacy concatenation formulas, and V1's newly explicit N2-support failure was not propagated into V13's closed terminal-status vocabulary.

Blocking findings: V5 and V13, with the exact replacement text in Findings 4 and 12 above.
